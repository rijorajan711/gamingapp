import React from 'react'

function Reset({sendOnclick}) {
  return (
    <div>

        <button className='Reset' onClick={sendOnclick}>TO RESET</button>
      
    </div>
  )
}

export default Reset
